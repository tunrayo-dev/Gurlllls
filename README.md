# Girls Unplugged — targeted fixes

This folder contains the updated shared navigation, FAQ behaviour, seven editable team slots, privacy page, and an images folder.

Image placeholders are filename/path based. Add your images inside `images/` and edit the paths in `navigation.js` if needed.
